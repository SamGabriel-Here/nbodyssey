# Benchmarks

Kernel timing and energy-vs-time logs land here.

- `timing_*.csv` — per-step and aggregate force-kernel timing (CUDA events)
- `energy_*.csv` — total system energy vs simulation time (correctness diagnostic)
